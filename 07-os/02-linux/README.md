# Linux

Practical, structured notes for learning, configuring, managing, troubleshooting, and monitoring **Linux systems**.

## Contents

|#|Topic|What You'll Learn|
|---|---|---|
|00|Quick Notes|Short Linux notes, quick facts, commands, and frequently referenced information|
|01|Basics|Linux distributions, kernel, shell, terminal, and package management fundamentals|
|02|Filesystem|Linux filesystem hierarchy and important directories such as `/`, `/home`, `/etc`, `/var`, `/usr`, `/opt`, `/tmp`, and `/dev`|
|03|Users & Groups|Users, groups, account management, group management, and `sudo`|
|04|Permissions|File permissions, `chmod`, `chown`, `umask`, ACLs, and privilege management|
|05|Processes|Processes, `ps`, `top`, `kill`, signals, jobs, and process priorities|
|06|Services|`systemd`, `systemctl`, service units, `journald`, and service management|
|07|Storage|Disks, partitions, mounting, `/etc/fstab`, LVM, and disk usage|
|08|Networking|Network interfaces, IP addressing, routing, DNS, SSH, and firewalls|
|09|Boot|Linux boot process, GRUB, initramfs, and systemd-boot|
|10|Packages|APT, `dpkg`, Snap, Flatpak, and package repositories|
|11|Monitoring|CPU, memory, disk, network, and overall system monitoring|
|—|Toolkits|Linux tools, utilities, commands, and practical tool collections|

## Structure

The notes progress from **Linux fundamentals → filesystem and user management → permissions and processes → services and storage → networking and boot → package management → system monitoring**.

The `00-quick-notes/` section contains short, quickly referenced information, while the numbered sections provide structured learning material. `TOOLKITS-LINUX.md` serves as a practical collection of useful Linux tools and utilities.

The later sections focus increasingly on system administration, including services, storage, networking, boot processes, packages, and system resource monitoring.

Each topic is separated into focused notes for easier learning, reference, troubleshooting, and expansion.

## Focus

- Linux distributions and fundamentals
- Linux kernel, shell, and terminal
- Package management
- Linux filesystem hierarchy
- Users and groups
- `sudo` and privilege management
- File permissions and ACLs
- Processes, signals, and jobs
- Process priorities
- `systemd` and service management
- `systemctl` and `journald`
- Disks, partitions, and mounting
- `/etc/fstab` and LVM
- Disk usage and storage management
- Network interfaces and IP configuration
- Routing and DNS
- SSH and firewall management
- Linux boot process
- GRUB and initramfs
- APT, `dpkg`, Snap, and Flatpak
- CPU, memory, disk, and network monitoring
- Linux administration tools and utilities

## Goal

> Build a practical Linux knowledge base that makes it easy to understand, configure, administer, troubleshoot, and monitor Linux systems in everyday development and system administration work.
